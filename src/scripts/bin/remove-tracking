#!/usr/bin/env bash
# Wrapper script for remove-tracking Python utility

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PYTHON_SCRIPT="$SCRIPT_DIR/../cleanup_wordml/remove-tracking.py"

python "$PYTHON_SCRIPT" "$@"
