#!/usr/bin/env bash
# Wrapper script for remove-noproof Python utility

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PYTHON_SCRIPT="$SCRIPT_DIR/../cleanup_wordml/remove-noproof.py"

python "$PYTHON_SCRIPT" "$@"
